# frozen_string_literal: true

source 'https://rubygems.org'

ruby '>= 3.0'

# Všechny závislosti jsou ze standardní knihovny Ruby:
# csv, json, time, net/http, uri, optparse, yaml, digest, fileutils, openssl, date
# Gemfile je připraven pro budoucí přidání gemů (faraday, zeitwerk, atd.)
